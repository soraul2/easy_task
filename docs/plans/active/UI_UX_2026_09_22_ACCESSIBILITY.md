# UI·UX 접근성 감사 원본 54건 분류

원본 54건의 분류표. 요소를 식별할 수 없는 지적은 해결된 결함으로 계산하지 않는다. 원본: `.local/uiux-audit-20260922/attachments/`. 상세 좌표·색상·픽셀 수와 원본 파일 연결: `.local/uiux-20260922/accessibility-classification.json`.

대비 값은 원본 스크린샷의 글자 내부색과 해당 노드 배경의 대표색을 sRGB 상대휘도로 계산했다. 안티앨리어싱 경계색은 글자색으로 쓰지 않았다. 같은 노드에 여러 색/배경이 섞이는 경우 전체 화면과 테마 규칙 검사를 함께 검토한다.

이름과 좌표를 제공하지 않는 자동 지적은 특정 요소로 추정해 없애지 않는다. 같은 조건 재감사와 최대 글자/상세 이동 관찰로 제품 문제 여부를 따로 확인하며 원문은 보존한다.

| 원본 ID | 종류 | 원본 이름 | 분류 | 조치/근거 |
|---|---|---|---|---|
| board-01 | 터치 영역 | 보통 | 표시 전용 정보에 대한 터치 영역 지적 | 유지. MobileTaskMetadataLabel에는 동작/버튼이 없고 상태 변경·편집은 별도 44pt 이상 버튼이다. |
| board-02 | 터치 영역 | 예상 30분 | 표시 전용 정보에 대한 터치 영역 지적 | 유지. MobileTaskMetadataLabel에는 동작/버튼이 없고 상태 변경·편집은 별도 44pt 이상 버튼이다. |
| board-03 | 대비 | 1 | 실제 내부 글자색 대비 통과 / 자동 지적과 불일치 | 유지. 동일 화면 내부 글자색과 배경색 대비 14.74:1. 테마 15개 규칙 검사도 통과. 색 토큰 변경 근거가 없어 유지. |
| board-04 | 대비 | 오늘의 계획 | 실제 내부 글자색 대비 통과 / 자동 지적과 불일치 | 유지. 동일 화면 내부 글자색과 배경색 대비 6.84:1. 테마 15개 규칙 검사도 통과. 색 토큰 변경 근거가 없어 유지. |
| board-05 | 대비 | 오늘 | 실제 내부 글자색 대비 통과 / 자동 지적과 불일치 | 유지. 동일 화면 내부 글자색과 배경색 대비 16.05:1. 테마 15개 규칙 검사도 통과. 색 토큰 변경 근거가 없어 유지. |
| board-06 | 대비 | 보통 | 실제 내부 글자색 대비 통과 / 자동 지적과 불일치 | 유지. 동일 화면 내부 글자색과 배경색 대비 6.84:1. 테마 15개 규칙 검사도 통과. 색 토큰 변경 근거가 없어 유지. |
| board-07 | 대비 | 예상 30분 | 실제 내부 글자색 대비 통과 / 자동 지적과 불일치 | 유지. 동일 화면 내부 글자색과 배경색 대비 6.84:1. 테마 15개 규칙 검사도 통과. 색 토큰 변경 근거가 없어 유지. |
| board-08 | 대비 | (이름·영역 미제공) | 자동 감사가 요소/영역을 제공하지 않은 지적 | 식별 한계 기록 · 임의 수정 없음. 같은 조건 재감사에서도 이름/좌표가 없는 지적이 남는다. 특정 휴일·일정·시스템 요소로 단정하지 않는다. 전체 화면·AX5와 날짜 상세 접근 검증을 연결하고 원본을 보존한다. |
| board-09 | 잘림 가능성 | 2026.09.22 화 | 확대 시 적응형 날짜 헤더 | 유지. AX5의 appleSystem/charcoalRose에서 날짜 전체 표시와 가로 영역 포함 검사 통과. phone-regression/attachments/E4B96305-9B09-40B9-8D36-91AEC6EB60EA.png 및 1E9FF350-36D6-4FD7-ADC5-64777CCEF4BD.png. |
| board-10 | 잘림 가능성 | 진행 중 | 확대 시 상태 메뉴로 전환 | 유지. 일반 글자에서는 진행 중 전체 문구를 표시. AX5에서는 MobileBoardStatusFilter.accessibilityStatusMenu로 전환하며 상태·개수 전체 접근성 값을 제공한다. AX5 메뉴 조작 가능 검사 및 완료/취소 흐름 통과. 실제 VoiceOver 발화는 별도 인수. |
| calendar-01 | 대비 | 수 | 실제 내부 글자색 대비 통과 / 자동 지적과 불일치 | 유지. 동일 화면 내부 글자색과 배경색 대비 6.89:1. 테마 15개 규칙 검사도 통과. 색 토큰 변경 근거가 없어 유지. |
| calendar-02 | 대비 | 30 | 실제 내부 글자색 대비 통과 / 자동 지적과 불일치 | 유지. 동일 화면 내부 글자색과 배경색 대비 6.66:1. 테마 15개 규칙 검사도 통과. 색 토큰 변경 근거가 없어 유지. |
| calendar-03 | 대비 | 31 | 실제 내부 글자색 대비 통과 / 자동 지적과 불일치 | 유지. 동일 화면 내부 글자색과 배경색 대비 6.66:1. 테마 15개 규칙 검사도 통과. 색 토큰 변경 근거가 없어 유지. |
| calendar-04 | 대비 | 1 | 실제 내부 글자색 대비 통과 / 자동 지적과 불일치 | 유지. 동일 화면 내부 글자색과 배경색 대비 16.05:1. 테마 15개 규칙 검사도 통과. 색 토큰 변경 근거가 없어 유지. |
| calendar-05 | 대비 | 2 | 실제 내부 글자색 대비 통과 / 자동 지적과 불일치 | 유지. 동일 화면 내부 글자색과 배경색 대비 16.05:1. 테마 15개 규칙 검사도 통과. 색 토큰 변경 근거가 없어 유지. |
| calendar-06 | 대비 | 3 | 실제 내부 글자색 대비 통과 / 자동 지적과 불일치 | 유지. 동일 화면 내부 글자색과 배경색 대비 16.05:1. 테마 15개 규칙 검사도 통과. 색 토큰 변경 근거가 없어 유지. |
| calendar-07 | 대비 | 4 | 실제 내부 글자색 대비 통과 / 자동 지적과 불일치 | 유지. 동일 화면 내부 글자색과 배경색 대비 16.05:1. 테마 15개 규칙 검사도 통과. 색 토큰 변경 근거가 없어 유지. |
| calendar-08 | 대비 | 5 | 실제 내부 글자색 대비 통과 / 자동 지적과 불일치 | 유지. 동일 화면 내부 글자색과 배경색 대비 16.05:1. 테마 15개 규칙 검사도 통과. 색 토큰 변경 근거가 없어 유지. |
| calendar-09 | 대비 | 6 | 실제 내부 글자색 대비 통과 / 자동 지적과 불일치 | 유지. 동일 화면 내부 글자색과 배경색 대비 15.93:1. 테마 15개 규칙 검사도 통과. 색 토큰 변경 근거가 없어 유지. |
| calendar-10 | 대비 | 7 | 실제 내부 글자색 대비 통과 / 자동 지적과 불일치 | 유지. 동일 화면 내부 글자색과 배경색 대비 15.93:1. 테마 15개 규칙 검사도 통과. 색 토큰 변경 근거가 없어 유지. |
| calendar-11 | 대비 | 8 | 실제 내부 글자색 대비 통과 / 자동 지적과 불일치 | 유지. 동일 화면 내부 글자색과 배경색 대비 15.93:1. 테마 15개 규칙 검사도 통과. 색 토큰 변경 근거가 없어 유지. |
| calendar-12 | 대비 | 9 | 실제 내부 글자색 대비 통과 / 자동 지적과 불일치 | 유지. 동일 화면 내부 글자색과 배경색 대비 15.93:1. 테마 15개 규칙 검사도 통과. 색 토큰 변경 근거가 없어 유지. |
| calendar-13 | 대비 | 10 | 실제 내부 글자색 대비 통과 / 자동 지적과 불일치 | 유지. 동일 화면 내부 글자색과 배경색 대비 15.93:1. 테마 15개 규칙 검사도 통과. 색 토큰 변경 근거가 없어 유지. |
| calendar-14 | 대비 | 11 | 실제 내부 글자색 대비 통과 / 자동 지적과 불일치 | 유지. 동일 화면 내부 글자색과 배경색 대비 15.93:1. 테마 15개 규칙 검사도 통과. 색 토큰 변경 근거가 없어 유지. |
| calendar-15 | 대비 | 12 | 실제 내부 글자색 대비 통과 / 자동 지적과 불일치 | 유지. 동일 화면 내부 글자색과 배경색 대비 15.93:1. 테마 15개 규칙 검사도 통과. 색 토큰 변경 근거가 없어 유지. |
| calendar-16 | 대비 | 13 | 실제 내부 글자색 대비 통과 / 자동 지적과 불일치 | 유지. 동일 화면 내부 글자색과 배경색 대비 15.93:1. 테마 15개 규칙 검사도 통과. 색 토큰 변경 근거가 없어 유지. |
| calendar-17 | 대비 | 14 | 실제 내부 글자색 대비 통과 / 자동 지적과 불일치 | 유지. 동일 화면 내부 글자색과 배경색 대비 15.93:1. 테마 15개 규칙 검사도 통과. 색 토큰 변경 근거가 없어 유지. |
| calendar-18 | 대비 | 15 | 실제 내부 글자색 대비 통과 / 자동 지적과 불일치 | 유지. 동일 화면 내부 글자색과 배경색 대비 15.93:1. 테마 15개 규칙 검사도 통과. 색 토큰 변경 근거가 없어 유지. |
| calendar-19 | 대비 | 16 | 실제 내부 글자색 대비 통과 / 자동 지적과 불일치 | 유지. 동일 화면 내부 글자색과 배경색 대비 15.93:1. 테마 15개 규칙 검사도 통과. 색 토큰 변경 근거가 없어 유지. |
| calendar-20 | 대비 | 17 | 실제 내부 글자색 대비 통과 / 자동 지적과 불일치 | 유지. 동일 화면 내부 글자색과 배경색 대비 15.93:1. 테마 15개 규칙 검사도 통과. 색 토큰 변경 근거가 없어 유지. |
| calendar-21 | 대비 | 18 | 실제 내부 글자색 대비 통과 / 자동 지적과 불일치 | 유지. 동일 화면 내부 글자색과 배경색 대비 15.93:1. 테마 15개 규칙 검사도 통과. 색 토큰 변경 근거가 없어 유지. |
| calendar-22 | 대비 | 19 | 실제 내부 글자색 대비 통과 / 자동 지적과 불일치 | 유지. 동일 화면 내부 글자색과 배경색 대비 15.93:1. 테마 15개 규칙 검사도 통과. 색 토큰 변경 근거가 없어 유지. |
| calendar-23 | 대비 | 20 | 실제 내부 글자색 대비 통과 / 자동 지적과 불일치 | 유지. 동일 화면 내부 글자색과 배경색 대비 15.93:1. 테마 15개 규칙 검사도 통과. 색 토큰 변경 근거가 없어 유지. |
| calendar-24 | 대비 | 21 | 실제 내부 글자색 대비 통과 / 자동 지적과 불일치 | 유지. 동일 화면 내부 글자색과 배경색 대비 15.93:1. 테마 15개 규칙 검사도 통과. 색 토큰 변경 근거가 없어 유지. |
| calendar-25 | 대비 | (이름·영역 미제공) | 자동 감사가 요소/영역을 제공하지 않은 지적 | 식별 한계 기록 · 임의 수정 없음. 같은 조건 재감사에서도 이름/좌표가 없는 지적이 남는다. 특정 휴일·일정·시스템 요소로 단정하지 않는다. 전체 화면·AX5와 날짜 상세 접근 검증을 연결하고 원본을 보존한다. |
| calendar-26 | 대비 | (이름·영역 미제공) | 자동 감사가 요소/영역을 제공하지 않은 지적 | 식별 한계 기록 · 임의 수정 없음. 같은 조건 재감사에서도 이름/좌표가 없는 지적이 남는다. 특정 휴일·일정·시스템 요소로 단정하지 않는다. 전체 화면·AX5와 날짜 상세 접근 검증을 연결하고 원본을 보존한다. |
| calendar-27 | 대비 | 27 | 실제 내부 글자색 대비 통과 / 자동 지적과 불일치 | 유지. 동일 화면 내부 글자색과 배경색 대비 15.90:1. 테마 15개 규칙 검사도 통과. 색 토큰 변경 근거가 없어 유지. |
| calendar-28 | 대비 | 28 | 실제 내부 글자색 대비 통과 / 자동 지적과 불일치 | 유지. 동일 화면 내부 글자색과 배경색 대비 15.90:1. 테마 15개 규칙 검사도 통과. 색 토큰 변경 근거가 없어 유지. |
| calendar-29 | 대비 | 29 | 실제 내부 글자색 대비 통과 / 자동 지적과 불일치 | 유지. 동일 화면 내부 글자색과 배경색 대비 15.90:1. 테마 15개 규칙 검사도 통과. 색 토큰 변경 근거가 없어 유지. |
| calendar-30 | 대비 | 30 | 실제 내부 글자색 대비 통과 / 자동 지적과 불일치 | 유지. 동일 화면 내부 글자색과 배경색 대비 15.90:1. 테마 15개 규칙 검사도 통과. 색 토큰 변경 근거가 없어 유지. |
| calendar-31 | 대비 | 1 | 실제 내부 글자색 대비 통과 / 자동 지적과 불일치 | 유지. 동일 화면 내부 글자색과 배경색 대비 6.49:1. 테마 15개 규칙 검사도 통과. 색 토큰 변경 근거가 없어 유지. |
| calendar-32 | 대비 | (이름·영역 미제공) | 자동 감사가 요소/영역을 제공하지 않은 지적 | 식별 한계 기록 · 임의 수정 없음. 같은 조건 재감사에서도 이름/좌표가 없는 지적이 남는다. 특정 휴일·일정·시스템 요소로 단정하지 않는다. 전체 화면·AX5와 날짜 상세 접근 검증을 연결하고 원본을 보존한다. |
| calendar-33 | 대비 | 2 | 실제 내부 글자색 대비 통과 / 자동 지적과 불일치 | 유지. 동일 화면 내부 글자색과 배경색 대비 6.49:1. 테마 15개 규칙 검사도 통과. 색 토큰 변경 근거가 없어 유지. |
| calendar-34 | 대비 | 3 | 실제 내부 글자색 대비 통과 / 자동 지적과 불일치 | 유지. 동일 화면 내부 글자색과 배경색 대비 6.49:1. 테마 15개 규칙 검사도 통과. 색 토큰 변경 근거가 없어 유지. |
| calendar-35 | 대비 | (이름·영역 미제공) | 자동 감사가 요소/영역을 제공하지 않은 지적 | 식별 한계 기록 · 임의 수정 없음. 같은 조건 재감사에서도 이름/좌표가 없는 지적이 남는다. 특정 휴일·일정·시스템 요소로 단정하지 않는다. 전체 화면·AX5와 날짜 상세 접근 검증을 연결하고 원본을 보존한다. |
| calendar-36 | 잘림 가능성 | (이름·영역 미제공) | 자동 감사가 요소/영역을 제공하지 않은 지적 | 식별 한계 기록 · 임의 수정 없음. 같은 조건 재감사에서도 이름/좌표가 없는 지적이 남는다. 특정 휴일·일정·시스템 요소로 단정하지 않는다. 전체 화면·AX5와 날짜 상세 접근 검증을 연결하고 원본을 보존한다. |
| calendar-37 | 잘림 가능성 | (이름·영역 미제공) | 자동 감사가 요소/영역을 제공하지 않은 지적 | 식별 한계 기록 · 임의 수정 없음. 같은 조건 재감사에서도 이름/좌표가 없는 지적이 남는다. 특정 휴일·일정·시스템 요소로 단정하지 않는다. 전체 화면·AX5와 날짜 상세 접근 검증을 연결하고 원본을 보존한다. |
| calendar-38 | 잘림 가능성 | (이름·영역 미제공) | 자동 감사가 요소/영역을 제공하지 않은 지적 | 식별 한계 기록 · 임의 수정 없음. 같은 조건 재감사에서도 이름/좌표가 없는 지적이 남는다. 특정 휴일·일정·시스템 요소로 단정하지 않는다. 전체 화면·AX5와 날짜 상세 접근 검증을 연결하고 원본을 보존한다. |
| calendar-39 | 잘림 가능성 | (이름·영역 미제공) | 자동 감사가 요소/영역을 제공하지 않은 지적 | 식별 한계 기록 · 임의 수정 없음. 같은 조건 재감사에서도 이름/좌표가 없는 지적이 남는다. 특정 휴일·일정·시스템 요소로 단정하지 않는다. 전체 화면·AX5와 날짜 상세 접근 검증을 연결하고 원본을 보존한다. |
| calendar-40 | 잘림 가능성 | (이름·영역 미제공) | 자동 감사가 요소/영역을 제공하지 않은 지적 | 식별 한계 기록 · 임의 수정 없음. 같은 조건 재감사에서도 이름/좌표가 없는 지적이 남는다. 특정 휴일·일정·시스템 요소로 단정하지 않는다. 전체 화면·AX5와 날짜 상세 접근 검증을 연결하고 원본을 보존한다. |
| records-01 | 대비 | (이름·영역 미제공) | 자동 감사가 요소/영역을 제공하지 않은 지적 | 식별 한계 기록 · 임의 수정 없음. 같은 조건 재감사에서도 이름/좌표가 없는 지적이 남는다. 특정 휴일·일정·시스템 요소로 단정하지 않는다. 전체 화면·AX5와 날짜 상세 접근 검증을 연결하고 원본을 보존한다. |
| records-02 | 대비 | (이름·영역 미제공) | 자동 감사가 요소/영역을 제공하지 않은 지적 | 식별 한계 기록 · 임의 수정 없음. 같은 조건 재감사에서도 이름/좌표가 없는 지적이 남는다. 특정 휴일·일정·시스템 요소로 단정하지 않는다. 전체 화면·AX5와 날짜 상세 접근 검증을 연결하고 원본을 보존한다. |
| records-03 | 잘림 가능성 | 작업 제목, 메모, 회고 검색 | 시스템 검색 힌트의 잠재적 잘림 | 힌트 간결화, 잠재적 지적은 보존. 최종 final-records-default 감사에는 짧은 기록 검색에도 UISearchBarTextField의 큰 글자 잠재적 잘림 지적이 남는다. 실제 AX5 화면(48F91C26-E00D-437D-8BB8-5AE417EB0DF3.png)에서 문구 전체가 보이고 입력/회전 검사도 통과해, 해당 시스템 요소를 숨기거나 글자 크기를 제한하지 않는다. |
| memo-01 | 대비 | 메모 없음 | 실제 내부 글자색 대비 통과 / 자동 지적과 불일치 | 유지. 동일 화면 내부 글자색과 배경색 대비 15.64:1. 테마 15개 규칙 검사도 통과. 색 토큰 변경 근거가 없어 유지. |

자동 감사의 수집 성공을 접근성 준수 또는 VoiceOver 실제 발화 검증으로 해석하지 않는다.

## 재감사 및 해석

- 최종 기본 화면 수집은 **54건**: 칸반 10, 캘린더 40, 기록 4, 메모 0. 중간 실행 53건과 구분한다. 짧아진 기록 검색에도 시스템 검색 바의 잠재적 잘림 지적은 남지만 실제 AX5 화면에서는 전체 문구가 보인다. 원본보다 이름/좌표 없는 대비 지적 1건이 늘고 메모의 대비 지적 1건이 없어졌다. 숫자만으로 결함 수나 개선율을 계산하지 않는다.
- 원본의 이름·영역 없는 12건(대비 7, 잘림 가능성 5)은 도구의 식별 한계로 남겼다. 특정 휴일이나 시스템 구성요소로 임의 분류하지 않았다.
- 캘린더 AX5는 월 전체 격자에서 일정 제목 대신 막대 요약을 사용하며, 날짜 상세에서 전체 일정 제목·기간·내용을 읽고 수정할 수 있다. appleSystem/charcoalRose의 월 이동·날짜 상세·기록 필터 검사를 통과했다. 관련 화면은 `phone-regression/attachments/`의 `adaptive-AX5-…-calendar-month/day` 이름으로 연결된다.
- 검색 바 힌트는 시스템 구성요소다. `Text.foregroundStyle(AppTheme.secondaryText)`를 시험했지만 동일 위치의 내부 글자색(115, 115, 115)이 전후 동일해 해당 변경을 제외했다. 원본에서 요소를 제공하지 않은 지적을 이 검색 바로 단정하지 않는다. OS 최대 글자의 메모 검색 힌트는 (132,132,136)/(239,239,240)으로 측정됐으며, 색 대비 수치만으로 화면 전체 접근성 적합을 선언하지 않는다.
- 보존한 raw 파일: `phone-final-probes/attachments/C8413A11-D922-460D-ABF6-07946EB0D792.txt`(칸반), `72451D57-363F-4C7E-BCD4-A96A983FC4F4.txt`(캘린더), `B0F7CF6E-AC14-4BB2-9E74-1FC4A75AEF52.txt`(기록), `E7337B1C-4073-48D1-8689-5C382EFCD607.txt`(메모). 위 상대 경로의 기준은 `.local/uiux-20260922/`다.

최종 소스 raw 파일(`final-records-default/attachments/`): `2DA2A76A-A6B2-429A-A53D-66EB4338C364.txt`(칸반), `BE2E3ED1-A047-4D52-9DB4-2BCFDBE626A3.txt`(캘린더), `9A69633F-AA36-4BE6-BCE3-07861F5299D2.txt`(기록), `6D349518-E460-4C74-9B2A-413742311583.txt`(메모). 원본·중간 실행도 삭제하지 않았다.
